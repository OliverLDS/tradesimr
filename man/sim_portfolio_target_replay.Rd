% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_bulk_replay.R
\name{sim_portfolio_target_replay}
\alias{sim_portfolio_target_replay}
\title{Replay a historical multi-asset target-weight panel}
\usage{
sim_portfolio_target_replay(
  exchange,
  bars,
  target_weights,
  allowed_symbols = NULL,
  execution = sim_portfolio_execution(),
  decision_policy = sim_portfolio_decision_policy(),
  rebalance_policy = NULL,
  export_path = NULL,
  profile = FALSE,
  production_calendar = FALSE
)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{bars}{Completed OHLC bars with \code{timestamp}, \code{symbol}, \code{asset_id},
\code{open}, \code{high}, \code{low}, and \code{close}. Each timestamp is one market boundary.}

\item{target_weights}{A data frame with \code{timestamp}, \code{agent_id}, \code{symbol},
and \code{target_weight}. Each agent/timestamp group is submitted atomically.}

\item{allowed_symbols}{Optional named list of allowed-symbol vectors by
agent. When omitted, each agent's universe is inferred from all symbols in
its panel rows. Multi-asset target groups are submitted only at boundaries
containing one completed bar for every allowed symbol; incomplete groups
are treated as absent decisions.}

\item{execution}{Execution assumptions from \code{\link[=sim_portfolio_execution]{sim_portfolio_execution()}}.}

\item{decision_policy}{Market-observation policy from
\code{\link[=sim_portfolio_decision_policy]{sim_portfolio_decision_policy()}}. Applied independently to each agent
decision after the market boundary has been accepted.}

\item{rebalance_policy}{Optional policy for sparse deterministic target
panels. \code{NULL} (the default) preserves historical behavior and submits
every agent/timestamp target group. When supplied, it must be a list with
\code{rebalance_due_column} (default \code{"rebalance_due"}) and a non-negative
\code{drift_tolerance}. A group is submitted only when its due flag is \code{TRUE}
and either its target vector differs from the last submitted vector or a
supplied symbol's post-market realized-weight drift exceeds the tolerance.
A skipped group is an absent decision: it creates no target, rebalance, or
order record.}

\item{export_path}{Optional directory for public-safe per-agent exports.}

\item{profile}{Whether to return wall-time categories.}

\item{production_calendar}{When \code{TRUE}, require an explicit exchange
\code{calendar_mode} of \code{"calendarize"} or \code{"strict"}; production replay may
not silently use legacy raw-bar admission.}
}
\value{
A list with the exchange, durable orders/fills/positions/accounts,
targets/rebalances, execution quality, optional export paths, and timings.
}
\description{
This bulk API is intended for historical reconstruction. It preserves the
public market-boundary and target-submission semantics while accumulating
durable snapshots and events in batches rather than repeatedly growing their
history tables. Live callers should continue to use
\code{\link[=sim_portfolio_market_step]{sim_portfolio_market_step()}} and \code{\link[=sim_portfolio_target_submit_batch]{sim_portfolio_target_submit_batch()}}.
}
