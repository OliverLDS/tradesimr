% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_replay.R
\name{sim_portfolio_target_submit}
\alias{sim_portfolio_target_submit}
\title{Submit one Arena target-weight decision after a market boundary}
\usage{
sim_portfolio_target_submit(
  exchange,
  agent_id,
  bars,
  target_weights = NULL,
  execution = sim_portfolio_execution(),
  decision_label = "target_weight",
  allowed_symbols = NULL,
  allowed_asset_ids = NULL
)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{agent_id}{Account identifier. Each agent has an isolated account.}

\item{bars}{The already accepted timestamped completed-bar batch.}

\item{target_weights}{Optional named numeric target weights keyed by
registered symbols.}

\item{execution}{Execution assumptions from \code{sim_portfolio_execution()}.}

\item{decision_label}{Optional durable label for the decision source.}

\item{allowed_symbols}{Optional registered symbols this agent may target,
hold, or trade. Persisted on the agent after a successful submission.}

\item{allowed_asset_ids}{Optional registered asset ids this agent may target,
hold, or trade. When supplied with \code{allowed_symbols}, both must identify
the same allowed universe.}
}
\value{
A list containing \code{orders}, \code{fills}, \code{positions}, \code{account},
\code{targets}, \code{realized_weights}, and \code{outcomes}.
}
\description{
The supplied decision bars must already have been accepted by
\code{sim_portfolio_market_step()}. Targets are converted atomically using the
completed-bar close and the post-step account state. Submitted orders are
eligible only on a strictly later bar of the same asset. \code{NULL} records a
durable no-decision outcome and preserves current positions.
}
