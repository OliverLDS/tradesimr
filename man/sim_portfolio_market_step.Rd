% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_replay.R
\name{sim_portfolio_market_step}
\alias{sim_portfolio_market_step}
\title{Advance an Arena exchange at one completed market boundary}
\usage{
sim_portfolio_market_step(
  exchange,
  bars,
  execution = sim_portfolio_execution()
)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{bars}{One timestamped batch of registered, completed OHLC bars.}

\item{execution}{Execution assumptions from \code{sim_portfolio_execution()}.}
}
\value{
A public-safe list with \code{bars}, \code{fills}, \code{events}, \code{positions},
\code{account}, and \code{outcomes}.
}
\description{
This API advances a timestamped batch of completed bars exactly once. It
executes only orders submitted at earlier boundaries; it never creates a
target decision or rebalance. Each supplied asset bar must be genuinely new,
so duplicate and stale batches fail rather than being silently reprocessed.
}
