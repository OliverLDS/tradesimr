% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_step.R
\name{sim_step}
\alias{sim_step}
\title{Step the C++ exchange kernel once}
\usage{
sim_step(
  state,
  bar,
  orders = data.frame(),
  asset = state$asset \%||\% 0L,
  ctr_size = 1,
  ctr_step = 1,
  lev = 10,
  fee_rt = 0,
  maker_fee_rt = NA_real_,
  taker_fee_rt = NA_real_,
  fund_rt = 0,
  funding_interval_hours = 8,
  mmr = 0.02,
  slippage = 0,
  spread = 0,
  record = TRUE
)
}
\arguments{
\item{state}{Prior state created by \code{sim_state()} or returned by \code{sim_step()}.}

\item{bar}{One-row market bar coercible by \code{as_market_bars()}.}

\item{orders}{Data frame with order columns: \code{action}, \code{dir}, \code{order_type},
\code{ctr_qty}, \code{price}, and optional \code{strat_id}, \code{action_id}, and
\code{fee_aware_target}. The last flag is used internally for target-position
and target-weight orders, whose opening quantity is capped at the fill
price to reserve fees and initial margin.}

\item{asset}{Integer asset identifier.}

\item{ctr_size}{Contract size.}

\item{ctr_step}{Minimum contract increment.}

\item{lev}{Leverage used for initial margin.}

\item{fee_rt}{Trading fee rate on notional. Fees are reserved by
target-derived opening/increase actions at their fill boundary; explicit
contract orders continue to fail if their requested quantity is infeasible.}

\item{maker_fee_rt, taker_fee_rt}{Optional maker/taker fee rates. Missing
values fall back to \code{fee_rt}.}

\item{fund_rt}{Funding rate per 8 hours on notional.}

\item{funding_interval_hours}{Funding interval in hours.}

\item{mmr}{Maintenance margin rate.}

\item{slippage}{Absolute slippage added against trade direction.}

\item{spread}{Absolute bid/ask spread; half spread is added against trade
direction.}

\item{record}{Whether to attach the execution recorder.}
}
\value{
A list with \code{state} and \code{events}.
}
\description{
Processes one bar and a batch of explicit order actions against prior account
state. This is the incremental primitive underneath future live exchange
workflows; it does not replay earlier bars.
}
