% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_backtest.R
\name{sim_backtest}
\alias{sim_backtest}
\title{Run a stateful trading simulation backtest}
\usage{
sim_backtest(
  data,
  timestamp_col = "timestamp",
  open_col = "open",
  high_col = "high",
  low_col = "low",
  close_col = "close",
  tgt_pos_col = "tgt_pos",
  pos_strat_col = NULL,
  tol_pos_col = NULL,
  order_type_col = NULL,
  limit_price_col = NULL,
  strat = 0L,
  asset = 0L,
  init_cash = 10000,
  ctr_size = 1,
  ctr_step = 1,
  lev = 10,
  fee_rt = 0,
  maker_fee_rt = NA_real_,
  taker_fee_rt = NA_real_,
  fund_rt = 0,
  funding_interval_hours = 8,
  mmr = 0.02,
  fill_model = c("next_open", "same_close"),
  slippage = 0,
  spread = 0,
  tol_pos = 0,
  record = TRUE
)
}
\arguments{
\item{data}{A data frame/data.table with timestamp, open, high, low, close,
and target-position columns.}

\item{timestamp_col, open_col, high_col, low_col, close_col, tgt_pos_col}{Column
names in \code{data}.}

\item{pos_strat_col}{Optional strategy-id column. If absent, \code{strat} is used.}

\item{tol_pos_col}{Optional target-position tolerance column. If absent,
\code{tol_pos} is used.}

\item{order_type_col}{Optional order type column using \code{market} or \code{limit}.}

\item{limit_price_col}{Optional limit price column.}

\item{strat, asset}{Integer identifiers for the simulation and asset.}

\item{init_cash}{Initial account cash.}

\item{ctr_size}{Contract size.}

\item{ctr_step}{Minimum contract increment.}

\item{lev}{Leverage used for initial margin.}

\item{fee_rt}{Trading fee rate on notional. Fees are reserved by
target-derived opening/increase actions at their fill boundary; explicit
contract orders continue to fail if their requested quantity is infeasible.}

\item{maker_fee_rt, taker_fee_rt}{Optional maker/taker fee rates. Missing
values fall back to \code{fee_rt}.}

\item{fund_rt}{Funding rate per 8 hours on notional.}

\item{funding_interval_hours}{Funding interval in hours.}

\item{mmr}{Maintenance margin rate.}

\item{fill_model}{Fill timing model: \code{next_open} or \code{same_close}.}

\item{slippage}{Absolute slippage added against trade direction.}

\item{spread}{Absolute bid/ask spread; half spread is added against trade
direction.}

\item{tol_pos}{Scalar default target-position tolerance used when
\code{tol_pos_col} is absent.}

\item{record}{Whether to attach the execution recorder.}
}
\value{
A data.table with timestamp and equity. If \code{record = TRUE}, an
execution recorder is attached as attribute \code{orders}.
}
\description{
\code{sim_backtest()} executes target-position intentions through the package's
C++ exchange/accounting engine. Orders are planned at bar close and market
orders are filled on the next bar open. Target-derived opening and increase
orders are fee-aware: they are clipped at the executable price to the
largest step-rounded quantity satisfying \code{equity - fee >= initial_margin}.
Thus a \code{tgt_pos} of \code{1} at \code{lev = 1} produces the largest near-100\%-notional
position that reserves its transaction fee rather than a failed order.
}
