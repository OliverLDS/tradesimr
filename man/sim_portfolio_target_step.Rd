% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_replay.R
\name{sim_portfolio_target_step}
\alias{sim_portfolio_target_step}
\title{Step one agent portfolio from target weights}
\usage{
sim_portfolio_target_step(
  exchange,
  agent_id,
  bars,
  target_weights = NULL,
  execution = sim_portfolio_execution(),
  decision_label = "target_weight"
)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{agent_id}{Account identifier. Each agent has an isolated account.}

\item{bars}{One timestamped batch of registered, completed OHLC bars.}

\item{target_weights}{Optional named numeric target weights keyed by
registered symbols.}

\item{execution}{Execution assumptions from \code{sim_portfolio_execution()}.}

\item{decision_label}{Optional durable label for the decision source.}
}
\value{
A list containing \code{orders}, \code{fills}, \code{positions}, \code{account},
\code{targets}, \code{realized_weights}, and \code{outcomes}.
}
\description{
This is the stable Vox Arena integration point. It accepts a batch of
genuinely new completed OHLC bars and, optionally, an explicit target-weight
decision. Existing eligible orders are stepped first. A new target decision
is then translated atomically into contract orders using current account
equity and the decision-bar close (or the latest carried valuation for an
asset absent from the batch). New orders are eligible only on a strictly
later bar for their own asset. At that later fill boundary, target-derived
opening/increase orders are capped to the largest step-rounded quantity whose
fee and initial margin fit the account. Consequently a target weight of \code{1}
at \code{lev = 1} is near 100\% notional after reserving the fee, rather than a
failed all-cash order. Explicit contract orders retain reject-on-insufficient
margin semantics.
}
\details{
Missing target weights for active registered assets mean a target weight of
zero. A \code{NULL} target is a no-decision: positions are retained and no new
orders are submitted. Repeated/stale bars are ignored, so closed markets can
retain their last valuation without creating strategy reactions or fills.
}
