% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_replay.R
\name{sim_portfolio_target_submit_batch}
\alias{sim_portfolio_target_submit_batch}
\title{Submit multiple Arena target-weight decisions after one market boundary}
\usage{
sim_portfolio_target_submit_batch(
  exchange,
  bars,
  decisions,
  execution = sim_portfolio_execution(),
  decision_policy = sim_portfolio_decision_policy()
)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{bars}{The already accepted timestamped completed-bar batch.}

\item{decisions}{A named list keyed by \code{agent_id}.}

\item{execution}{Execution assumptions from \code{sim_portfolio_execution()}.}

\item{decision_policy}{Market-observation policy applied to every decision.}
}
\value{
A list with the common boundary \code{timestamp}, public account and
position snapshots, and a named \code{submissions} list containing the same
result contract as \code{sim_portfolio_target_submit()} for each agent.
}
\description{
This is the efficient replay interface for a common completed market
boundary. It does not step market data. Instead, it validates the accepted
boundary once and snapshots account, position, and carried-price state once
before translating each agent's decision atomically. Each \code{decisions}
element is a named list containing \code{target_weights} (or \code{NULL} for a
no-decision), and optionally \code{decision_label}, \code{allowed_symbols}, and
\code{allowed_asset_ids}. A multi-asset decision requires exactly one completed
bar for every allowed asset at the common decision boundary.
}
