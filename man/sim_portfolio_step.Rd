% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_step.R
\name{sim_portfolio_step}
\alias{sim_portfolio_step}
\title{Step the C++ portfolio-margin kernel once}
\usage{
sim_portfolio_step(
  states,
  bars,
  orders = data.frame(),
  cov = diag(nrow(as_market_bars(bars))),
  shared_cash = 10000,
  ctr_size = 1,
  ctr_step = 1,
  lev = 10,
  fee_rt = 0,
  maker_fee_rt = NA_real_,
  taker_fee_rt = NA_real_,
  fund_rt = 0,
  funding_interval_hours = 8,
  mmr = 0.02,
  portfolio_margin_sigma = 3,
  portfolio_margin_floor = mmr,
  slippage = 0,
  spread = 0,
  record = TRUE
)
}
\arguments{
\item{states}{Named list of prior \code{sim_state()} objects keyed by \code{asset_id}.}

\item{bars}{One timestamp batch of market bars.}

\item{orders}{Order data frame with \code{asset_id}, \code{action}, \code{dir},
\code{order_type}, \code{ctr_qty}, \code{price}, \code{strat_id}, and \code{action_id}.}

\item{cov}{Return covariance matrix aligned to \code{bars$asset_id}.}

\item{shared_cash}{Shared account cash before this step.}

\item{ctr_size, ctr_step}{Contract size and quantity step. Supply one value
for all assets or one value per row of \code{bars}, aligned to \code{asset_id}.}

\item{lev}{Leverage used for initial margin.}

\item{fee_rt}{Trading fee rate on notional. Fees are reserved by
target-derived opening/increase actions at their fill boundary; explicit
contract orders continue to fail if their requested quantity is infeasible.}

\item{maker_fee_rt, taker_fee_rt}{Optional maker/taker fee rates. Missing
values fall back to \code{fee_rt}.}

\item{fund_rt}{Funding rate per 8 hours on notional.}

\item{funding_interval_hours}{Funding interval in hours.}

\item{mmr}{Maintenance margin rate.}

\item{portfolio_margin_sigma}{Sigma multiplier for covariance margin.}

\item{portfolio_margin_floor}{Floor margin rate applied to gross exposure.}

\item{slippage}{Absolute slippage added against trade direction.}

\item{spread}{Absolute bid/ask spread; half spread is added against trade
direction.}

\item{record}{Whether to attach the execution recorder.}
}
\value{
A list with \code{states}, \code{cash}, \code{equity}, \code{maintenance_margin},
\code{liquidated}, and \code{events}.
}
\description{
Processes one timestamp batch of bars and explicit orders for one agent under
one shared cash balance. This is the multi-asset primitive used by live
exchanges when \code{portfolio_margin = TRUE}.
}
