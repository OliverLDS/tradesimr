% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_portfolio_execution_quality.R
\name{sim_portfolio_execution_quality}
\alias{sim_portfolio_execution_quality}
\title{Project execution quality for durable portfolio target rebalances}
\usage{
sim_portfolio_execution_quality(exchange, agent_id = NULL, summary = FALSE)
}
\arguments{
\item{exchange}{A \code{tradesimr_exchange}.}

\item{agent_id}{Optional account identifier used to filter the projection.}

\item{summary}{If \code{FALSE} (the default), return one row per rebalance and
symbol. If \code{TRUE}, return one rebalance-level row whose quality is the
worst component quality.}
}
\value{
A public-safe data.table. Symbol rows contain \code{rebalance_id},
\code{agent_id}, \code{symbol}, \code{asset_id}, \code{decision_timestamp}, \code{eligible_after},
\code{settlement_timestamp}, \code{target_weight}, \code{decision_equity},
\code{decision_price}, \code{qty_step}, \code{contract_size},
\code{current_signed_quantity}, \code{expected_signed_quantity},
\code{expected_notional}, \code{realized_signed_quantity}, \code{realized_notional},
\code{quantity_deviation}, \code{notional_deviation}, \code{weight_deviation},
\code{execution_quality}, and \code{message}.
}
\description{
The projection evaluates each target using its decision-time target record,
canonical order lifecycle, durable fills, and the position/account snapshot
at the relevant settlement boundary. It never uses a later current mark to
classify an earlier rebalance. Filled fee-aware target orders are assessed
against their exact C++-recorded executable quantity.
}
